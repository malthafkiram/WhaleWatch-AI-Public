# Prefer setting Railway Root Directory to "server/" (recommended).
# This root file is a fallback if Root Directory is the monorepo root.
[build]
builder = "nixpacks"

[deploy]
startCommand = "npm start"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
